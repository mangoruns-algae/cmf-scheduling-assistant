# CMF 排班助手更新日志

本文件记录系统各版本的功能、界面和部署变更。

## 版本编号规则

- 当前版本为 `0.2.6`。
- 系统正式上线前，第一位和第二位固定为 `0.2`。
- 每次更新只增加第三位，例如：`0.2.1` → `0.2.2` → `0.2.9` → `0.2.10`。
- 除非项目负责人明确通知，第一位和第二位版本号不作更改。
- 每次更新必须同时修改网页页脚版本号、更新日期、README 和本更新日志。

## 0.2.6 — 2026.08.25

### Supabase 配置与提示修复

- Supabase Secrets 增加 `[supabase]` 分组配置支持，避免配置被错误写入 `[accounts]`。
- 保留根层级 `SUPABASE_URL`、`SUPABASE_KEY` 和 `SUPABASE_BUCKET` 的兼容读取。
- 修复上传失败后页面立即重新运行，导致错误信息无法被用户看到的问题。
- 上传成功后通过会话提示明确显示“已永久保存到 Supabase”。

## 0.2.5 — 2026.08.25

### Supabase 永久存储

- 将多排班看板从 Streamlit 进程内存迁移到 Supabase。
- 原始 Excel 和系统生成的 Excel 保存到私有 `schedule-files` Storage bucket。
- 文件名、日期范围、工作项数量、上传人、状态和哈希保存到 `schedules` 表。
- 看板页面每次启动从 Supabase 重新加载，应用休眠或重新部署后数据不再丢失。
- 使用文件哈希执行去重与更新，避免相同文件重复创建记录。
- 创建者“从看板移除”调整为数据库归档，避免直接删除原始文件。
- Supabase 未配置或连接失败时明确报错，不再静默退回临时内存保存。
- 新增 `SUPABASE_SETUP.sql`，用于安全初始化及升级现有数据表。

## 0.2.4 — 2026.08.24

### PDF 宽度修复

- 修复长排班为了适配单页而被整体等比例缩小、导致表格集中在页面左侧的问题。
- 改用固定 A4 可用宽度的绘制方式，表格始终横向铺满页面内容区。
- 只根据内容量调整字号、行高和单元格内部换行，不再缩小整张表格宽度。
- 增加单元格内容裁切保护，避免长文本越过表格边界。
- 保留 A4 纵向、单页输出、动态列宽和人员高亮规则。

## 0.2.3 — 2026.08.24

### 上传格式自动优化

- 上传排班后自动标准化五列表格，不覆盖用户原始 Excel 文件。
- 人员姓名统一使用顿号分隔，并清理多余空格、换行和混合标点。
- 对没有逗号或顿号的连续中文姓名，依据中文姓氏边界自动拆分。
- 日期、时间、现场负责人和人员安排统一居中；工作内容统一左对齐。
- 所有单元格统一垂直居中、自动换行和内边距。

### PDF 排版

- PDF 页面由 A3 横向调整为 A4 纵向单页。
- 中文优先使用微软雅黑，英文和数字优先使用 Arial；运行环境缺少字体时自动使用兼容字体。
- 字号与行高根据排班总行数自动缩放。
- 五列宽度根据各列内容长度动态分配，同时保留最低可读宽度。

## 0.2.2 — 2026.08.24

### 多排班看板

- 将单一已发布班表扩展为多排班共享看板。
- 支持一次选择并上传多份生产排班 Excel。
- 支持分多次继续追加排班原文件，同一文件不会重复暂存。
- 每份排班以卡片形式展示文件名、日期范围、工作项数量和上传人。
- 增加文件名、批次号和日期关键词搜索。
- 支持在多份排班之间切换，并继续使用人员定位及单页 PDF 导出。
- 创建者可以从临时看板中移除不再需要的排班。
- 看板采用 Streamlit 实例内临时共享存储，应用重启、休眠恢复或重新部署后清空。

## 0.2.1 — 2026.08.24

### 账户与权限

- 新增访客账户 `Kaili_Cui`。
- 该账户仅具备排班预览、个人班次定位和 PDF 导出权限。
- 登录后自动进入排班预览者界面，不显示排班创建者选项。

## 0.2.0 — 2026.08.24

### 账户与权限

- 增加多账户登录，用户名匹配不区分大小写。
- `Qingyuan_Qin` 和 `admin` 可使用排班创建者及排班预览者模式。
- `Nicole_Shi` 和 `Biao_Jiao` 为只读预览账户，登录后默认进入预览界面。
- 在左侧栏当前账户下方增加权限清单和当前发布状态。

### 排班创建与发布

- 保留原有人员、可用性、用工需求和排班规则数据结构。
- 保留原有排班算法、缺员提示、跨产线借调和 Excel 导出功能。
- 增加“锁定并发布班表”操作。
- 创建者可以发布系统生成的排班，也可以上传并发布现有生产排班 Excel。
- 预览者可直接查看创建者当前锁定的班表。

### 班表预览与导出

- 分析并兼容现有生产排班 Excel 的日期、时间、生产内容、现场负责人和人员安排五列结构。
- 保留原模板的主要布局、合并单元格和配色风格。
- 增加按姓名定位个人班次功能。
- 本人参与的工作使用黄色整行高亮。
- 本人担任现场负责人时，使用蓝色单独标记日期。
- 同一天未参与的其他工序不再被连带高亮。
- PDF 排班表限制为单页输出。

### 界面与部署

- 标题更新为“CMF 排班助手”，增加 Henlius 公司 Logo。
- 优化标题对齐、字号、页面间距、表格可读性、按钮层级和整体视觉一致性。
- 增加版本号、更新日期和 Powered by Qingyuan 页脚。
- 增加 Streamlit Community Cloud 部署支持和 GitHub 自动部署流程。

## 0.1.0 — 2026.08.21

### 初始版本

- 建立 Streamlit 排班 MVP。
- 支持人员信息、人员可用性、用工需求和排班规则 Excel 导入。
- 支持人员资格、可用性、本产线优先、跨线借调和夜班资格检查。
- 支持基础公平分配和缺员提示。
- 支持人员与日期班表视图。
- 支持排班结果 Excel 导出。
- 增加基础账户登录页。
- 完成 GitHub 与 Streamlit Community Cloud 初始部署。
