# CMF 排班助手

当前版本：**0.2.5**  
更新日期：**2026.08.25**

## 访问网址

- 在线应用：[https://cmf-scheduling-assistant.streamlit.app](https://cmf-scheduling-assistant.streamlit.app)
- GitHub 项目：[https://github.com/mangoruns-algae/cmf-scheduling-assistant](https://github.com/mangoruns-algae/cmf-scheduling-assistant)
- 本地运行地址：[http://127.0.0.1:8501](http://127.0.0.1:8501)

## 平台简介

- **Streamlit**：一个用 Python 快速构建数据应用网页的平台；本项目使用它展示登录、排班创建、班表预览、人员定位和文件下载界面，并通过 Streamlit Community Cloud 提供在线访问。
- **GitHub**：一个用于保存和管理代码版本的平台；本项目使用 GitHub 存放源代码，并在代码更新后触发 Streamlit Cloud 自动重新部署。

## 使用方法

### 1. 登录

打开在线应用，输入分配的账户和密码。用户名不区分大小写。

- `Qingyuan_Qin`、`admin`：可创建、发布和预览排班。
- `Nicole_Shi`、`Biao_Jiao`、`Kaili_Cui`：仅可预览已发布排班、定位个人班次和导出 PDF。

### 2. 创建并发布排班

1. 使用具有创建权限的账户登录。
2. 选择“排班创建者”。
3. 上传基于 `staff_scheduling_template.xlsx` 填写的数据文件。
4. 检查人员资源、用工需求和排班规则。
5. 点击“生成排班”。
6. 检查排班结果后，点击“锁定并发布班表”。

仅生成但未锁定的班表不会显示给预览者。

### 3. 预览班次

1. 选择“排班预览者”；只读账户登录后会自动进入该页面。
2. 在已排班看板中搜索并选择需要查看的排班。
3. 创建者可以一次选择多个 Excel，也可以分多次上传并暂存到看板。
4. 在“人员定位”中选择姓名：
   - 黄色整行表示该人员参与该项工作；
   - 蓝色日期表示该人员担任现场负责人；
   - 同一天未参与的其他工序不会高亮。
5. 点击“导出 A4 单页排班 PDF”保存查阅版本。

### 4. 本地运行

可双击 `start_app.cmd`，或者在项目目录执行：

```bash
pip install -r requirements.txt
streamlit run app.py
```

然后访问 [http://127.0.0.1:8501](http://127.0.0.1:8501)。

## 当前注意事项

- Streamlit Cloud 免费实例可能因长时间无人访问而休眠，首次打开需要等待唤醒。
- 排班原文件保存在 Supabase 私有 Storage，排班元数据保存在 `schedules` 表。
- Streamlit 休眠、重启或重新部署后，看板会重新从 Supabase 加载，不再丢失。
- Supabase Secret Key 只能保存在 Streamlit Secrets，不能提交到 GitHub。

## 版本规则

- 系统正式上线前，第一位和第二位版本号固定为 `0.2`。
- 后续每次功能或界面更新只增加第三位：`0.2.1`、`0.2.2`……`0.2.9`、`0.2.10`。
- 除非项目负责人明确通知，第一位和第二位均不修改。
- 每次修改必须同步更新网页页脚、README 和 `CHANGELOG.md` 中的版本号及日期。

## 更新历史

完整记录见 [CHANGELOG.md](CHANGELOG.md)。

### 0.2.5 — 2026.08.25

- 排班原始 Excel 和系统生成班表接入 Supabase 永久存储。
- 看板从 Supabase 数据库读取，Streamlit 重启后仍可恢复。
- 增加重复文件覆盖更新、私有文件下载、归档和连接错误提示。
- 新增可重复执行的 `SUPABASE_SETUP.sql` 初始化/升级脚本。

### 0.2.4 — 2026.08.24

- 修复 A4 单页 PDF 因等比例缩放而只占页面左侧的问题。
- PDF 表格现在固定铺满 A4 可用宽度，仅动态压缩字号和行高。
- 使用单元格裁切和自动换行，避免长内容越界或相互覆盖。

### 0.2.3 — 2026.08.24

- 上传排班后自动统一人员姓名分隔、单元格对齐、换行和间距。
- 自动识别缺少逗号或顿号的连续中文姓名，并统一显示为顿号分隔。
- PDF 改为 A4 纵向单页，中文优先使用微软雅黑、英文优先使用 Arial。
- PDF 字号、行高和列宽根据排班行数及内容长度动态调整。

### 0.2.2 — 2026.08.24

- 增加多排班看板，可集中展示多份已排班文件。
- 支持一次批量上传或分多次追加排班 Excel。
- 增加文件名和日期搜索、看板卡片、排班切换及创建者移除功能。

### 0.2.1 — 2026.08.24

- 新增访客账户 `Kaili_Cui`。
- 该账户仅可预览已发布排班、定位个人班次和导出 PDF。

### 0.2.0 — 2026.08.24

- 增加账户权限、创建者和只读预览者角色。
- 增加班表锁定发布、已发布班次预览和人员定位。
- 增加单页 PDF 导出及参与人员、现场负责人的差异化高亮。
- 优化登录页、标题、Logo、页面信息层级和表格可读性。

### 0.1.0 — 2026.08.21

- 完成人员、可用性、用工需求和规则的 Excel 导入。
- 完成基础自动排班、缺员提示和 Excel 结果导出。
- 完成项目登录页和 Streamlit Cloud 初始部署。
